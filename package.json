{
  "name": "dydat",
  "private": true,
  "scripts": {
    "dev:backend": "pnpm --filter ./apps/backend run start:dev",
    "dev:frontend": "pnpm --filter ./apps/frontend run dev",
    "build": "pnpm --filter ./apps/* run build"
  },
  "devDependencies": {
    "@nestjs/typeorm": "^10.0.2",
    "typeorm": "^0.3.20",
    "typescript": "^5.4.5",
    "prettier": "^3.2.5"
  }
} 